{
  "name": "derby",
  "description": "Reactive MVC framework with automatically synced models and views. Supports server and client rendering of multi-user, realtime applications.",
  "version": "0.0.8",
  "main": "./lib/derby.js",
  "browserify": {
    "main": "./lib/derby.browser.js"
  },
  "dependencies": {
    "racer": "0.0.13",
    "uglify-js": ">=1.0.6",
    "stylus": ">=0.15",
    "nib": ">=0.2"
  },
  "devDependencies": {
    "expresso": ">=0.7.9",
    "should": ">=0.2.1",
    "coffee-script": ">=1.1.1",
    "express": ">=2.4.2"
  },
  "engines": {
    "node": ">=0.4.0"
  }
}
