{
  "name": "derby",
  "description": "Framework for creating realtime applications with automatically synced models and functionally defined views. HTML and updates are rendered both on the server and on the client.",
  "version": "0.0.0",
  "main": "./lib/derby.js",
  "browserify": {
    "main": "./lib/derby.browser.js"
  },
  "dependencies": {
    "racer": "0.0.8",
    "uglify-js": ">=1.0.6"
  },
  "devDependencies": {
    "expresso": ">=0.7.9",
    "should": ">=0.2.1",
    "coffee-script": ">=1.1.1",
    "express": ">=2.4.2"
  },
  "engines": {
    "node": "*"
  }
}
